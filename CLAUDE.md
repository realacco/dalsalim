# 달살림 (dalsalim)

가족이 **한 달 살림을 폰으로 같이 적는** 가계부 앱. 엑셀로 쓰던 가족 가계부를 대체한다.

카드 내역을 자동으로 긁어오지 않는다. 초기에 등록한 **사람별 고정비 항목이 매달 템플릿**이 되고,
버튼 하나로 지난달 값이 채워진 채 열리며, **금액이 달라진 줄에만 사유를 필수로 받는다.**

> **지금은 MVP 단계다.** 빠르게 만들어 몇 달 써보고 방향을 잡는 게 목적이지, 완성도가 목적이 아니다.
> "나중에 필요할지도"로 지금 만드는 것은 전부 범위 밖이다.

## 문서 체계 (SSOT 규칙)

- **제품 결정** = `docs/01-MVP-기획서.md` — 기획서와 코드가 다르면 **기획서가 맞다.**
  기획을 바꾸려면 기획서를 먼저 고치고 코드를 바꾼다.
- **출시 조건** = `docs/02-MVP-출시-체크리스트.md` — "출시라는 사건에 무엇이 필요한가"
- **시행착오 기록** = `docs/03-MVP-시행착오.md` — 실제로 데인 것들. 같은 함정을 다시 밟지 않기 위한 기록
- **작업 목록** = `docs/MVP-개발-태스크.md` — "코드에 무슨 일을 하는가". **gitignore 대상**(로컬 전용)
- **데이터 모델 원본** = `server/prisma/schema.prisma` — 기획서 8장은 요약일 뿐이다
- **개발 환경·함정** = `README.md` 하단 "개발 노트"
- 기획서의 장 번호(3장, 7.5 …)를 코드 주석·커밋·대화의 공용어로 쓴다.

## 폴더 구조

### 서버 — 레이어드 (도메인 슬라이스로 쪼개지 않는다)

```
server/src/
  index.ts        부팅 — 플러그인 · 전역 에러 핸들러 · 라우트 등록. 로직 없음
  env.ts          환경변수 파싱 (여기가 유일한 process.env 접근 지점)
  routes/         HTTP 경계 — zod 검증 · 권한 확인 · 응답 조립. 얇게
  services/       도메인 로직 — 도메인당 한 파일 (entry · book · …)
  lib/            기반 — db(prisma) · auth(가드) · http(에러) · shared(상수·순수함수)
```

- **임포트 방향은 위에서 아래로만**: `routes → services → lib`.
  services가 routes를 임포트하거나, **라우트끼리 서로 임포트하는 것 금지.**
- **`lib/shared.ts`는 순수해야 한다.** prisma·fastify를 임포트하지 않는다.
  앱의 `entities/*/model`과 짝을 이루는 계약(카테고리 목록, `needsReason`, `yearMonth` 계산)이 여기 산다.
- **`process.env`는 `env.ts`에서만 읽는다.** 다른 파일에서 직접 읽지 않는다.
- **앱처럼 FSD로 쪼개지 않는다.** 도메인이 5개(session·family·fixed-expense·book·entry)로 고정이고
  `routes/`·`services/`가 이미 그 축으로 갈려 있다. 여기서 또 슬라이스를 파면
  파일 하나짜리 폴더만 늘어난다. **앱은 화면이 계속 늘지만 서버는 안 는다** — 그 차이가 구조를 가른다.

**라우트가 두꺼워지는 신호** (= services로 내릴 때다)

- 같은 계산을 두 라우트가 한다 → 그 순간 `services/`로 뺀다 (요약과 추이의 집계가 이 경우다)
- 라우트 핸들러 하나가 40줄을 넘는다
- 라우트 안에서 `prisma`를 세 번 이상 부른다 — 트랜잭션 경계가 흐려진 신호다

### 앱 — FSD (Feature-Sliced Design)

```
mobile/src/
  app/          expo-router 라우트 전용 — 얇게. screens를 re-export만 한다
  screens/      화면 단위 (FSD pages 레이어. 기획서 7장의 화면과 1:1)
  widgets/      화면 조각 (여러 feature 조합)
  features/     사용자 행동 단위 기능
  entities/     도메인 모델 (book, entry, fixed-expense, family, session)
  shared/       전역 공용 — ui / config(theme) / lib / api
```

- **임포트 방향은 위에서 아래로만**: `app → screens → widgets → features → entities → shared`.
  아래 레이어가 위 레이어를 임포트하거나, **같은 레이어끼리 서로 임포트하는 것 금지.**
- **`src/app/`은 라우팅만 한다.** 화면 구현을 여기 두지 않는다. 파일 하나에 `export { default } from '...'` 수준.
  (expo-router는 `src/app/`을 라우트 루트로 인식한다)
- **슬라이스 내부는 `ui/ model/ api/ lib/`로 나눈다.** 파일이 하나뿐일 땐 단독 파일도 허용하되,
  두 번째 관심사가 생기는 순간 폴더로 쪼갠다.

  | 종류 | 위치 | 규칙 |
  |---|---|---|
  | 화면 상태 조립 | 슬라이스의 `model/use-*.ts` | React 훅. 화면 하나에 훅 하나가 기본 |
  | 도메인 규칙·계산 | `entities/<domain>/model/*.ts` | **순수 함수. react·react-native 임포트 금지** |
  | 서버 통신 | `entities/<domain>/api/*.ts` | 화면에서 `api()` 직접 호출 금지. 쿼리 키도 여기서 관리 |
  | 범용 유틸 | `shared/lib` | 도메인 지식이 없는 것만. 섞이면 entities로 |

- **빈 레이어(widgets/features)는 필요해질 때 만든다.** 미리 만들지 않는다.
- 서버 응답 타입은 그 도메인의 `entities/<domain>/model/types.ts`에 둔다. 한 파일에 전부 모아두지 않는다.
  여러 도메인이 함께 쓰는 조각은 `shared/model/types.ts`로 — entities끼리는 임포트할 수 없다.

## 도메인 하드룰 (기획서 1·3장 — 절대 규칙)

이 앱이 다른 가계부와 갈라지는 지점이다. **어기면 제품이 아니라 다른 앱이 된다.**

1. **자동 "분류"를 만들지 않는다.** 상호명만으로 분류되는 건 10건 중 3건뿐이라
   나머지 7건은 어차피 사람이 고친다. 반쯤 분류해놓고 나머지를 떠넘기지 않는다. (기획서 1-3)
   - 내역을 **모으는 것**(카드사 문자 읽기)은 금지가 아니라 **보류**다 — iOS에서 불가능해
     가족이 둘로 갈리고, 카드사 형식 변화를 상시 따라갈 인력이 없다. 조건은 기획서 1-3.
   - 모으더라도 **분류는 끝까지 사람이 한다.** 앱은 "지난번에 여기를 어디에 넣으셨어요"까지만 말한다.
   - 손으로 적는 길을 **절대 없애지 않는다.** 수집은 원래 새는 것이라 보조여야 한다.
2. **차이에만 이유를 묻는다.** 기본값과 금액이 같으면 아무것도 묻지 않는다.
   다를 때만 사유를 **필수로** 받고, 되돌리면 사유도 같이 지운다.
3. **사유 강제의 최종 판정은 서버다.** `lib/shared.ts`의 `needsReason()` 한 곳에서만 판단하고,
   줄 저장(`PATCH /entries/:id/lines/:lineId`)과 제출(`POST /entries/:id/submit`) **양쪽에서** 막는다.
   앱 쪽 검사는 UX용이지 방어선이 아니다.
4. **`EntryLine`은 그 달의 스냅샷이다.** `name`·`category`는 `FixedExpense`에서 **복사**해 둔다.
   참조로 바꾸지 말 것 — 항목 이름을 바꾸면 과거 기록이 흔들린다.
   반대로 `fixedExpenseId`는 참조로 남긴다 (이름이 바뀌어도 같은 항목이라는 근거).
5. **금액은 원 단위 정수(`Int`)다.** 부동소수·통화 라이브러리를 들이지 않는다.
6. **과거를 지우지 않는다.** 고정비 삭제는 `active=false`, 멤버 제외는 `status='LEFT'`.
   실제 삭제는 과거 장부의 합계를 바꾼다.
   (예외: 승인 전 `PENDING` 은 매달린 기록이 없으므로 거절·취소 시 실제로 지운다)
7. **완성 판정은 `services/book.ts`의 `refreshBookStatus()` 한 곳에서만** 한다.
   그리고 그 상태는 **표시일 뿐 잠금장치가 아니다** — 요약·추이는 미제출자가 있어도 열린다. (기획서 3장)
8. **가족에 들어오려면 가족장이 승인해야 한다.** 초대코드는 카톡으로 오가다 새어나가는 문자열이라,
   코드를 맞힌 것만으로 가계부가 열리면 안 된다. 코드를 맞히면 `Membership.status='PENDING'`이고,
   `PENDING`은 **구성원 목록·장부 정원·모든 조회에서 빠진다** (`lib/shared.ts`의 `ACTIVE_MEMBER` 한 곳으로 판단).
   내보낸 사람이 코드로 되돌아오는 것도 같은 규칙으로 막힌다 — 재참여도 다시 승인을 받는다.
9. **판단하지 않는다.** 예산 초과 경고, 절약 점수, 등급, 가족 간 랭킹, "누가 더 썼나" 프레이밍 금지.
   달살림은 보여주기만 한다. 잔소리 앱이 되면 가족이 안 쓴다.

## 보안 규칙

- **`/auth/dev`는 개발 전용이다.** 이름만 알면 그 사람이 되는 라우트다.
  기본값은 꺼짐이고, 운영(`NODE_ENV=production`)에서는 라우트 자체가 등록되지 않는다.
- **`JWT_SECRET`이 기본값이면 운영에서 서버가 뜨지 않는다.** 조용히 도는 것보다 안 뜨는 게 낫다.
- **레이트리밋 키는 사용자 id 우선, 없으면 IP다** (`lib/auth.ts`의 `rateLimitKey`).
  한 집에서 공유기를 같이 쓰기 때문에 IP 로만 묶으면 가족끼리 서로의 한도를 갉아먹는다.
- **카카오 REST 키·client secret은 서버에만 둔다.** 앱 번들에 넣지 않는다
  (REST OAuth를 택한 이유의 절반이 이것이다).
- 토큰은 앱에서 `expo-secure-store`에만 저장한다. AsyncStorage·전역 변수 금지.
- 사용자가 입력한 가계 데이터는 **폰과 우리 서버 밖으로 나가지 않는다.**
  외부 분석·로깅 서비스, 외부 LLM API 어디에도 본문을 싣지 않는다.
  - **이미지 인식(OCR)을 하게 되면 온디바이스만.** 이미지를 클라우드로 보내지 않는다.
  - **카드사 문자 본문을 서버에 저장하지 않는다.** 파싱은 폰에서 하고,
    사용자가 확정한 금액만 `EntryLine` 으로 올라간다.
  - **예외는 아래 "LLM 사용 원칙"에 적힌 것뿐이다.** 거기 없으면 안 되는 것이다 —
    "그때 됐으니까 이것도"로 넓히지 않는다.
- `.env`를 커밋하지 않는다. 새 환경변수는 `server/.env.example`에 이름과 설명을 함께 추가한다.

## LLM 사용 원칙

| | 용도 | 왜 |
|---|---|---|
| ⭕ | **파싱 패턴 생성** — 실패 샘플로 정규식을 만든다 | **개발 도구지 런타임이 아니다.** 사용자 데이터가 안 나가고, 앱은 오프라인으로 돈다 |
| ⭕ | **붙여넣은 표 → 고정비 항목 변환** (온보딩 1회) | 정규식이 못 잡는 변형("약 12만", "연 82만 (8월)")을 흡수하는 **유일한 우위 지점** |
| ❌ | 상호명 → 카테고리 자동 분류 | 매 거래가 밖으로 나간다. 하드룰 1 |
| ❌ | 사유(`changeReason`) 자동 작성 | **기능의 의미를 파괴한다** — 아래 참조 |
| ⚠️ | 요약 문장화 | 가장 큰 차이 항목을 찾는 건 규칙 몇 줄이면 된다. LLM 값을 치를 이유가 없다 |

- **사유를 기계가 지어주면 안 된다.** 사유를 받는 이유는 칸을 채우는 게 아니라
  **적는 사람이 잠깐 멈춰서 생각하게 하는 것**이다. 그럴듯한 문장이 자동으로 들어가면
  아무도 안 읽고 넘기고, 몇 달 뒤 남는 건 아무 정보 없는 문장뿐이다.
- **⭕ 두 번째는 보안 규칙의 예외다.** 지킬 조건: 사용자가 직접 붙여넣고 버튼을 누를 때만 ·
  화면에 "이 내용을 분석 서버로 한 번 보냅니다"를 명시 · 결과를 표로 보여주고
  **확인·수정한 뒤에 저장** · 가계 기록(`EntryLine`)은 어떤 경우에도 안 보낸다.
- 자동 분류 대신 쓰는 것: **"지난번 선택 기억"** — 같은 상호를 지난달에 어디에 넣었는지 알려준다.
  폰 안에서만 돌고, 본인이 고른 거라 정확하고, 지출은 같은 곳을 반복하므로 두 달이면 대부분 채워진다.

## 디자인 시스템 규칙

- **스타일은 `StyleSheet` + `shared/config/theme.ts` 토큰으로만 쓴다.** NativeWind를 쓰지 않는다 —
  Babel·Metro·Tailwind 세 군데를 건드리는 비용이 화면 10개짜리 MVP에서는 이득보다 크다.
  이건 취향이 아니라 결정이므로, 바꾸려면 사용자에게 먼저 묻는다.
- **디자인 값 하드코딩 금지.** 색·간격·라운드·폰트 크기는 `shared/config/theme.ts`의
  `colors` / `space` / `radius` / `font` / `shadow`에서만 가져온다.
  `'#2E7D5B'`, `padding: 13`, `fontSize: 15` 같은 리터럴을 화면에 직접 쓰지 않는다.
  필요한 값이 없으면 **토큰을 먼저 추가**하고 쓴다.
- **색의 의미를 지킨다.** `up`(붉은색)은 "지출"이 아니라 **"지난달보다 늘어난 것"**이다.
  지출 자체는 잘못이 아니므로 지출에 경고색을 쓰지 않는다.
- 공용 컴포넌트는 `src/shared/ui/`
  (`Card` `Button` `Input` `AmountInput` `Field` `Row` `Chip` `ProgressBar` `Muted` `ErrorText` `Loading` `ScreenScroll` `SectionTitle` `Divider`).
  화면에서 같은 모양을 다시 만들기 전에 여기부터 본다.
- **`shared/config`의 토큰과 `shared/ui` 공용 컴포넌트의 수정·삭제는 사용자에게 먼저 허락받는다.**
  모든 화면에 파급된다. 신규 추가는 허락 없이 가능하되 추가 후 보고한다.

### 화면 원칙

- ⚠️ **입력 중에는 표시 형식을 바꾸지 않는다.** 컨트롤드 입력에서 우리가 문자열 길이를 바꾸면
  (3자리 콤마 같은) 커서가 밀리고 다음 글자가 엉뚱한 자리에 들어간다. **포맷은 포커스가 빠질 때 붙인다.**
  실제로 `800000`을 치면 `8,000,000`이 되는 버그가 있었다. (시행착오 1-1)
- **키보드를 피하는 건 화면 전체다.** `KeyboardAvoidingView`로 시트나 카드만 감싸면 줄어들 여지가
  없어서 주요 버튼이 그대로 덮인다. 바깥 컨테이너를 감싼다. (시행착오 1-5)
- **목록 화면에는 당겨서 새로고침을 단다.** 가족·장부는 다른 사람이 바꾸는 데이터다.
- **위저드는 한 화면에 질문 하나.** 스텝을 합치지 않는다. 이 앱에서 가장 많이 쓰이는 화면이다.
- **기본값이 이미 채워져 있는 상태가 정상이다.** 사용자가 할 일은 입력이 아니라 **다른 줄만 고치는 것**이다.
- 스텝을 넘길 때마다 서버에 저장한다(`cursor`). 앱을 꺼도 이어서 쓸 수 있어야 한다.
- 시스템 글자 크기를 키웠을 때, 폭 360dp에서 잘리지 않아야 한다. `allowFontScaling={false}` 금지.

## 실패 표현 · 응답 형태 · 말투 규칙 (M-6)

### 실패는 이렇게 보여준다 (앱)

| 상황 | 어떻게 | 무엇으로 |
|---|---|---|
| **조회 실패** (`useQuery` 의 `isError`) | 화면 자리에 카드 — 한 줄 설명 + **[다시 시도]** | `<QueryError error onRetry />` (`shared/ui`) |
| **저장 실패** — 폼 안 | 폼 아래 붉은 한 줄. **입력을 잃지 않는다** | `<ErrorText>` + `errorMessage(caught, MESSAGES.saveFailed)` |
| **동작 실패** — 버튼 하나짜리 (승인 · 내보내기 …) | Alert 한 번 | `Alert.alert(MESSAGES.actionFailed, errorMessage(caught, MESSAGES.actionFailedBody))` |
| **확인 다이얼로그** — 되돌리기 어려운 동작 앞 | 제목은 동사구 · 취소는 항상 왼쪽 · 실행 버튼은 제목의 동사 | `confirm({ title, body, confirmLabel, destructive })` (`shared/lib`) |
| **401** | 화면이 처리하지 않는다. `api()` 가 세션을 비우고 앱 셸이 로그인으로 보낸다 | `shared/api/client` 의 `onUnauthorized` — 한 곳 |
| **서버에 못 닿음** | 조회 실패와 같은 카드. 메시지에 주소가 붙는다 | `ApiError(0, 'NETWORK')` |

- **빈 화면을 남기지 않는다.** 서버가 죽었을 때 아무것도 안 보이면 사용자는 "데이터가 없구나"로 읽는다.
- 서버가 준 `message` 는 그대로 보여준다. 그 밖의 오류는 폴백 문장으로 — `errorMessage()` 한 곳에서 가른다.
- 폴백 · 재시도 · 확인 버튼 문구는 `shared/config/messages.ts` 에서 가져온다. 화면에 리터럴로 쓰지 않는다.

### 서버 응답 형태

- **조회**는 `{ 리소스명: … }` — `{ entry }` `{ family }` `{ requests }`. 배열도 이름을 붙인다.
- **만들거나 바꾼 리소스**는 그 리소스를 돌려준다 — `POST /families` → `{ family }`, `PATCH …/lines/:id` → `{ line }`.
  참여 요청은 대기 중인 멤버십을 만드는 것이므로 `{ membership }`, 초대코드 재발급은 가족을 바꾸는 것이므로 `{ family }`.
- **리소스가 없어지거나 리소스로 표현되지 않는 동작**은 `{ ok: true }` + 이어서 필요한 id — `{ ok: true, membershipId }`.
- 실패는 언제나 `{ code, message }`. `code` 는 앱과 스모크가 분기하는 계약이고, `message` 는 화면에 그대로 뜬다.
  `code` 는 `server/src/lib/messages.ts` 에만 정의한다 — 라우트에서 문자열을 새로 만들지 않는다.

### 말투

- **해요체.** `~해요` · `~예요` · `~해주세요`. `~습니다` 는 쓰지 않는다 — 서버 `message` 도 화면에 그대로 뜨므로 같다.
- **사용자를 탓하지 않는다.** "잘못 입력했습니다" ❌ → "초대코드는 6자리예요" ⭕. 상황을 말하고 끝낸다.
- **무엇을 하면 되는지 말한다.** "권한이 없습니다" ❌ → "가족장만 할 수 있어요" ⭕. 막힌 이유가 곧 다음 행동이다.
- 확인 버튼은 **제목의 동사**를 쓴다. "확인" · "예" 는 무엇을 확인하는지 안 보인다.

### 셀 수 있는 분리 기준

- 화면 파일에 **`useQuery` + `useMutation` 이 3개를 넘으면** `model/use-*.ts` 훅으로 내린다. (줄 수는 아무도 안 센다)
- 라우트 핸들러가 **`prisma` 를 3번 이상** 부르면 `services/` 로 내린다.

### 문서와 코드가 다를 때

- 임의로 맞추지 않는다. **어느 쪽이 맞는지 근거를 대고 사용자가 결정한다.** 기획 결정은 기획서가 맞고(SSOT),
  구현 세부는 코드가 맞을 수 있다. 결정이 나면 진 쪽을 고친다.

## 코드 규칙

- **TypeScript strict.** 서버·앱 양쪽 다 켜져 있다. `any`로 빠져나가지 않는다.
- **작업 완료 조건**: `npm test` + `npm run typecheck` + `npm run lint` + `node scripts/smoke.mjs`
  전부 통과 + **에뮬레이터에서 해당 화면 직접 확인.**
- **포맷은 Prettier 가 전담한다.** 손으로 맞추지 않는다 — 커밋할 때 바뀐 파일만 자동으로 정리된다.
  임의로 포맷을 바꾸는 변경을 로직 변경과 섞지 않는다 (포맷 전용 커밋으로 따로 낸다).
- **코드 식별자·파일명은 영어, 사용자에게 보이는 문구·주석·문서는 한국어.**
- **에러 메시지는 사용자에게 그대로 보여줄 수 있는 한국어로 쓴다.** `{ code, message }`에서
  `code`는 앱이 분기하는 용도, `message`는 화면에 그대로 뜬다.
  ("Validation failed" ❌ → "금액이 달라졌어요. 이번 달에 왜 이랬는지 한 줄만 적어주세요" ⭕)
- **주석은 "왜"만 쓴다.** "무엇을"은 코드가 말하게 한다.
  하드룰을 지키느라 돌아간 코드에는 사유 주석을 반드시 남긴다 (스냅샷 복사, soft delete 등).
- **컴포넌트는 그리기만 한다.** JSX 안에서 계산·정렬·포맷팅을 하지 않는다.
  금액 표시는 `shared/lib/format.ts`, 서버 통신은 해당 `entities/<domain>/api`를 거친다.
- **라우트는 얇게.** zod 검증 · 권한 확인 · 응답 조립까지만. 여러 라우트가 공유하는 계산은 `services/`로 내린다.
- **재사용 3회 규칙.** 같은 로직이 세 번째로 복사될 때 공용으로 올린다. 두 번까지는 복사를 허용한다.
- **삭제를 기본값으로.** 안 쓰는 코드·주석 처리된 코드를 남기지 않는다. 필요하면 git에서 되찾는다.
- **한 파일 = 한 책임.** 라우트 파일 200줄, 화면 파일 300줄, 함수 40줄을 넘으면 쪼갤 신호로 본다.
  (하드 리밋은 아니지만 넘길 때는 이유를 댈 수 있어야 한다.)

## 테스트 규칙

검증은 **3층**이고, 층마다 잡는 것이 다르다. 위층이 아래층을 대신하지 못한다.

| 층 | 도구 | 무엇을 | 언제 | 얼마나 |
|---|---|---|---|---|
| **1. 순수 함수** | **vitest** | 도메인 규칙 · 표시 형식 · **서버/앱 사본 일치** | 커밋마다 (pre-commit) | ~0.5초 |
| **2. API 흐름** | `server/scripts/smoke.mjs` | 사유 강제 · 승인 구조 · 장부 상태 전이 · 집계 범위 | 서버 로직을 바꾸면 · CI | ~10초 |
| **3. 화면** | **사람** (에뮬레이터) | 입력 가공 · 커서 · 키보드 | 그 화면을 건드릴 때 | — |

```
npm test          # 1층 전부 (루트에서)
npm run test:watch
cd server && node scripts/smoke.mjs   # 2층
```

- **3층은 자동화하지 않는다.** RN 화면 테스트는 유지비가 크고, 커서 밀림 같은 건
  결국 사람이 눌러봐야 안다 — 시행착오 1-1 이 그 증거다.
- ⚠️ **1층과 2층이 통과해도 화면은 따로 봐야 한다.** 서버가 받는 값이 늘 유효한 정수여도
  사용자가 친 것과 다를 수 있다. 금액·날짜처럼 **입력을 가공하는 화면은 반드시 에뮬레이터에서 눌러본다.**

### 어디에 무엇을 두나

| | 위치 | 무엇을 |
|---|---|---|
| 1층 · 서버 | `server/src/**/*.test.ts` | 소스 옆에 나란히 둔다 |
| 1층 · 앱 | `mobile/src/**/*.test.ts` | 같음. **`entities/*/model` 이 순수해야 하는 이유가 이것이다** |
| 1층 · 계약 | `tests/contract/*.test.ts` | ★ 서버와 앱에 **각각** 있는 같은 함수가 진짜 같은지 |
| 1층 · 도구 | `tests/tools/*.test.mjs` | 커밋 훅이 쓰는 스크립트 (비밀 스캔 규칙) |
| 2층 | `server/scripts/smoke.mjs` | 기획서 10장의 성공 기준 시나리오를 그대로 따라간다 |

### 지킬 것

- **하드룰을 건드리는 변경은 테스트에 케이스를 추가해야 완료다.** 예외 없다.
  특히: 사유 강제 · 사유 자동 삭제 · 프리필 우선순위 · 장부 상태 전이 · 요약 집계 범위
- 🔴 **서버와 앱에 같은 함수를 두면 `tests/contract` 에 일치 케이스를 같이 넣는다.**
  지금 세 쌍이다 — `needsReason` · `shiftYearMonth` · `CATEGORIES`.
  한쪽만 고치면 아무 경고 없이 갈라지고, 결과는 "저장은 되는데 [다음] 이 안 눌린다"처럼
  원인을 찾기 어려운 형태로 나타난다. 이 파일이 그걸 막는 **유일한** 자동 장치다.
- **버그 수정은 실패하는 케이스부터.** 재현 케이스를 먼저 넣어 빨간 것을 확인하고 고친다.
- **넣은 테스트는 일부러 깨뜨려 확인한다.** 코드를 비틀었을 때 빨개지지 않는 테스트는
  없는 것과 같다. (린트 규칙도 마찬가지다 — M-1 에서 실제로 데였다)
- **테스트는 몇 번이고 다시 돌 수 있어야 한다.** 한 번만 도는 스크립트는 곧 아무도 안 돌린다.
- 케이스 이름은 **한국어로 행동을 쓴다.** 하드룰을 지키는 케이스에는 `★` 를 붙인다.
  (`★ 금액이 달라지면 사유 없이는 막힌다`)
- **문구가 아니라 `code` 로 검증한다.** 에러 메시지는 말투가 바뀔 수 있지만 `code` 는 계약이다.
- 서버 로직을 바꾸면 **앱을 켜기 전에 스모크부터** 돌린다. 에뮬레이터로 디버깅하는 것보다 훨씬 빠르다.

## 커밋 규칙

형식은 Conventional Commits, 제목과 본문은 한국어로 쓴다.

```
type(scope): 한국어 한 줄 요약

왜 이렇게 했는지. 판단의 근거, 버린 대안, 알려진 한계.
diff를 읽으면 아는 "무엇을 바꿨는지"는 반복하지 않는다.
```

- **type** — `feat` / `fix` / `refactor` / `test` / `docs` / `style` / `perf` / `chore`
- **scope** — 변경의 무대. 아래 15개만 쓴다 (`commitlint.config.mjs` 가 막는다).
  여러 영역에 걸치면 생략한다. 자꾸 걸치면 커밋을 쪼갤 신호다.

  | 묶음 | scope | 무엇을 |
  |---|---|---|
  | **도메인 5** | `session` | 로그인 · 토큰 · 게이트 (`F-SES`) |
  | | `family` | 가족 · 초대코드 · 승인 · 구성원 (`F-FAM`) |
  | | `fixed-expense` | 고정비 항목 — 매달 템플릿이 되는 것 (`F-FIX`) |
  | | `book` | 월 장부 · 요약 · 추이 · 상태 (`F-BOOK`) |
  | | `entry` | 기록 · 줄 · 사유 · 제출 (`F-ENT`) |
  | **화면** | `wizard` | 입력 위저드. entry 도메인이지만 앱에서 가장 큰 슬라이스라 따로 부른다 |
  | | `ui` | `shared/ui` 공용 컴포넌트 · `shared/config` 디자인 토큰 |
  | **무대** | `server` `mobile` | 도메인 하나에 안 붙는 전반 변경 |
  | **도구** | `prisma` | 스키마 · 마이그레이션 |
  | | `lint` | eslint · prettier · husky · commitlint |
  | | `ci` | GitHub Actions |
  | | `deploy` | Railway · EAS |
  | | `deps` | 의존성 추가 · 갱신 |
  | **문서** | `plan` | 기획서 · 출시 체크리스트 · 태스크 문서 |

  도메인 5개는 기획서의 기능 ID · `server/routes` · `mobile/entities` 와 모두 같은 축이다.
  같은 변경이 `entry` 인지 `wizard` 인지 헷갈리면: **서버는 `entry`, 앱의 위저드 화면은 `wizard`.**
- **제목** — 50자 내외, 마침표 없음, 체언 종결(`~ 추가`, `~ 수정`). 코드 식별자는 영어 그대로.
- **본문은 "왜"를 쓴다.** 한 줄로 자명한 변경은 생략 가능하지만,
  판단이 들어간 변경(설계 선택, 우회, 트레이드오프)은 본문 필수.
- **한 커밋 = 한 가지 변경.** 포맷과 로직, 기능 추가와 리팩터링을 섞지 않는다.
- **깨진 상태를 커밋하지 않는다.** 커밋 전에 `typecheck` + 스모크를 통과시킨다.
- **기획 결정이 바뀐 커밋**은 본문에 기획서의 어느 장이 바뀌었는지 함께 적는다 (SSOT 규칙).
- **도메인 하드룰을 건드리는 커밋**은 본문에 준수 근거를 남긴다.
  (예: "name/category는 계속 복사한다 — 항목 이름 변경이 과거 기록을 흔들면 안 되므로")
- **브랜치 이름**은 `type/kebab-case` — `feat/trend-tab`, `fix/summary-gate`
- 에이전트가 작성한 커밋은 `Co-Authored-By` 트레일러를 남긴다.

### 커밋할 때 자동으로 도는 것 (husky)

| 훅 | 무엇을 | 왜 |
|---|---|---|
| `pre-commit` | **비밀 스캔** (`scripts/scan-secrets.mjs`) | 저장소가 PUBLIC 이다. 한 번 push 된 비밀은 히스토리를 다시 쓰기 전까지 남는다 |
| | 스테이징된 파일만 prettier + `eslint --fix` | 전체 lint 는 8초다. 느린 훅은 `--no-verify` 로 우회당한다 |
| | **1층 테스트** (vitest) | 0.5초라 매 커밋에 돌릴 값어치가 있다. 스모크는 CI 로 보낸다 |
| | **건드린 쪽만** typecheck | 양쪽 다 돌리면 8초, 한쪽이면 4초 |
| `commit-msg` | commitlint — type · scope · 제목 길이 · 마침표 · 본문 빈 줄 | 규칙을 문서에만 적어두면 지켜지는지 아무도 모른다 |

- **검사하지 않는 것**: 체언 종결(`~ 추가`) · 본문의 "왜" · 스모크.
  기계가 판정할 수 없어서 뺀 것이지 규칙이 아니어서가 아니다. 스모크는 CI 의 몫이다

### CI (GitHub Actions — `.github/workflows/ci.yml`)

훅이 "바뀐 것만" 보는 대신 CI 는 **전체**를 본다. PR 과 `main` push 에 돈다.

| job | 무엇을 | 왜 따로인가 |
|---|---|---|
| `check` | 포맷 검사 · 전체 lint · 전체 typecheck · 1층 테스트 | 훅을 `--no-verify` 로 우회한 커밋이 여기서 걸린다 |
| `smoke` | Postgres 컨테이너 → `migrate deploy` → 서버 기동 → 스모크 75개 | 훅에서는 DB 를 띄울 수 없다. **운영과 같은 마이그레이션 경로**를 매번 밟는다 |

- 두 job 은 서로 기다리지 않는다. 둘 다 빨간 것과 하나만 빨간 것은 원인이 다르다.
- CI 가 빨간 채로 머지하지 않는다. 로컬에서 `npm run lint && npm run typecheck && npm test` 와
  스모크를 돌리면 CI 와 같은 것을 본 것이다.
- 비밀 스캔이 자리표시자를 잘못 잡으면 그 줄 끝에 `secret-scan:allow` 를 적는다.
  규칙 자체를 고치면 `tests/tools/scan-secrets.test.mjs` 에 케이스를 같이 넣는다
- **`--no-verify` 는 쓰지 않는다.** 훅이 막았으면 막힐 이유가 있다

## 스키마 변경 규칙

- `prisma/schema.prisma`를 고치면 **기획서 8장의 표도 같이 고친다.**
- **스키마를 고치면 `npm run db:migrate`로 마이그레이션 파일을 만들고 반드시 커밋한다.**
  `prisma db push`(스키마 덮어쓰기)는 쓰지 않는다 — 데이터가 쌓인 뒤에 쓰면 컬럼이 날아간다.
  운영 배포는 `npm run db:deploy`(`migrate deploy`)다.
- 컬럼을 지우거나 이름을 바꾸는 변경은 사용자에게 먼저 확인한다.

## 명령어

```bash
# 저장소 루트 (커밋 훅 · 포매터)
npm install                    # husky 훅 설치까지 같이 된다. 클론하면 여기부터
npm test                       # 1층 테스트 (server · mobile · contract · tools)
npm run lint                   # server + mobile 전체
npm run typecheck              # tests + server + mobile 전체
npm run format                 # prettier --write .

# 서버
cd server
npm run dev              # tsx watch — 코드 고치면 자동 재시작 (localhost:4000)
npm run typecheck
node scripts/smoke.mjs   # 전 구간 검증 71개 (자기 가족 '스모크네'를 직접 만들어 쓴다)
npm run seed             # 데모 가족 '김씨네'
npm run db:studio        # Prisma Studio

# 앱
cd mobile
npm run dev -- --android # 에뮬레이터 부팅 + adb reverse + Metro
npm run typecheck
```

- 옵션: `--no-emu`(에뮬레이터 자동 실행 끄기) · `--clear`(Metro 캐시 초기화)
- ⚠️ **네이티브 빌드를 하지 않는다.** 카카오를 REST OAuth로 붙였기 때문에 `expo prebuild`도,
  `android/` 폴더도 없다. 네이티브 모듈이 필요한 라이브러리를 들이기 전에 먼저 상의한다 —
  Expo Go로 바로 도는 지금의 개발 루프를 잃는 결정이다.
- adb 먹통 · 에뮬레이터 프레임버퍼 정지 · 한글 입력 불가 등 함정은 `README.md` 개발 노트에 정리돼 있다.

## 현재 상태 (2026-08-31)

- 기획서 v0.1 기능 9개 중 **6개 구현 완료.** 남은 것: 추이 화면 · 요약 게이트 제거 · 멤버 관리
- 다음에 할 일은 `docs/MVP-개발-태스크.md`의 **1단계(요약 게이트 제거 + 집계 규칙 정정)**
- 카카오 키가 없어 **개발용 로그인만 동작**한다. 서버는 `adb reverse`로만 닿는다
- **DB는 Postgres다.** 로컬 개발도 Postgres를 본다 — SQLite는 배포 직전에 걷어냈다
- 에뮬레이터(`dalsalim` AVD)에서만 확인했다. **실기기는 한 번도 안 돌려봤다**
