node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# 네이티브 프로젝트는 prebuild 가 언제든 다시 만든다
android/
ios/

# 로컬 DB
*.db
*.db-journal

# 데이터 내보내기 (실제 가계 내용이 들어 있다 — 절대 커밋 금지)
data-export*.json

# 환경 변수 (server/.env.example 을 복사해서 쓴다)
.env
.env.local

*.log
npm-debug.*
.DS_Store
Thumbs.db

# 로컬 전용 문서 — 이 PC 에서만 뜻이 있는 것 (로컬 경로 · AVD 이름 · 진행 중 메모)
docs/MVP-개발-태스크.md
docs/개발-노트.md

# Claude Code 개인 설정 (팀 공유 설정은 .claude/settings.json)
.claude/settings.local.json

# 리뷰 워크플로가 만드는 파일
.github/review-context.md

# 다른 세션이 만드는 worktree
.claude/worktrees/
